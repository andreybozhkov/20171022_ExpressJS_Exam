<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>My Demo Server</title>
</head>
<body>
    <div class="container">
        <header>
            <ul class="menu">
                <li><a href="/">Home</a></li>
                <li><a href="/about">About</a></li>
                {{#if currentUser}}
                <li><a href="/">Authenticated user can view this</a></li>
                {{#if currentUser.isAdmin}}
                <li><a href="/">Admin can view this</a></li>
                {{/if}}
                <li>{{currentUser.username}}</li>
                <li>
                    <form id="logout-form" action="/logout" method="post"></form>
                    <a href="javascript:document.getElementById('logout-form').submit()">Logout</a>
                </li>
                {{else}}
                <li><a href="/loginRegister">Login/Register</a></li>
                {{/if}}
            </ul>
        </header>
        <main>
            {{#if successMessage}}
            <span>{{successMessage}}</span> {{/if}} 
            {{#if globalError}}
            <span>{{globalError}}</span> {{/if}} {{{body}}}
        </main>
    </div>
</body>

</html>